'use strict';

var fs = require('fs');
var hydrogenCodegen = require('@shopify/hydrogen-codegen');
var apiTypes = require('./api-types.js');
var getSchemaData = require('./helpers/get-schema-data.js');

const shopifyApiProject = ({ apiType, apiVersion, outputDir = ".", documents = ["**/*.{ts,tsx}", "!node_modules"], module, }) => {
    const { schema, schemaFile } = getSchemaData.getSchemaData(outputDir, apiType, apiVersion);
    const schemaFileExists = fs.existsSync(`${schemaFile}`);
    return {
        schema: schemaFileExists ? schemaFile : schema,
        documents,
        extensions: {
            codegen: {
                pluckConfig: hydrogenCodegen.pluckConfig,
                generates: apiTypes.shopifyApiTypes({
                    apiType,
                    apiVersion,
                    outputDir,
                    documents,
                    module,
                }),
            },
        },
    };
};

exports.shopifyApiProject = shopifyApiProject;
//# sourceMappingURL=api-project.js.map
